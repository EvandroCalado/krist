import { Meta, StoryFn } from '@storybook/react';

import { MenuUser } from '.';

export default {
  title: 'Components/ MenuUser',
  component: MenuUser,
} as Meta;

export const Default: StoryFn = () => <MenuUser />;
