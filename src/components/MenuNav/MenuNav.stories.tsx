import { Meta, StoryFn } from '@storybook/react';

import { MenuNav } from '.';

export default {
  title: 'Components/MenuNav',
  component: MenuNav,
} as Meta;

export const Default: StoryFn = () => <MenuNav />;
