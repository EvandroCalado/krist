import { Link } from 'react-router-dom';

export const Shop = () => {
  return (
    <div style={{ height: '50.5vh', textAlign: 'center' }}>
      <Link to="/login?transfer=/shop">login</Link>
    </div>
  );
};
