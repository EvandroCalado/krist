import styled from 'styled-components';

import {
  Container as LoginContainer,
  LoginForm,
  LoginImage,
  LogoContainer as LogoContainerLogin,
} from '../Login/Login.styles';

export const Container = styled(LoginContainer)``;

export const LogoContainer = styled(LogoContainerLogin)``;

export const RegisterImage = styled(LoginImage)``;

export const RegisterForm = styled(LoginForm)``;
